<div class="well">
<%= time_ago_in_words(answer.created_at) %> ago by <%= answer.user.email %><br>
	<%= answer.content %>

	<% if can? :update, answer %>
		<%= link_to 'Delete', question_answer_path(answer.question, answer), method: :delete, class:"btn btn-xs btn-danger" %>
	<% end %>	
		
</div>